#include <bits/stdc++.h>
using namespace std;

int main(){
    int x;
    double y;
    while (cin >> x) {
        y = x * 1.6;
        cout << fixed <<setprecision(1) << y << endl;
    }
    return 0;
}